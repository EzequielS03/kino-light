<?xml version="1.0" encoding="utf-8"?>
<!-- Debug-only additions. Nothing here reaches a release APK. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <!-- Triggered by hand over adb; see SondaDeCaracolOffline's KDoc. -->
        <receiver
            android:name="com.arkiv.player.debug.SondaDeCaracolOffline"
            android:exported="true" />

        <!-- Second question: does the shipping path work, queue and strategy included? -->
        <receiver
            android:name="com.arkiv.player.debug.PruebaDeDescargaDeCaracol"
            android:exported="true" />
    </application>
</manifest>
