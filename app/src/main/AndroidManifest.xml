<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-feature android:name="android.hardware.camera.any" android:required="false" />

    <!-- Android TV: no exigir pantalla táctil -->
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />
    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />

    <application
        android:name=".ArkivApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:banner="@drawable/ic_banner"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Arkiv"
        android:usesCleartextTraffic="true">

        <meta-data
            android:name="com.google.android.gms.cast.framework.OPTIONS_PROVIDER_CLASS_NAME"
            android:value="com.arkiv.player.cast.CastOptionsProvider" />

        <service
            android:name=".playback.PlaybackService"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaSessionService" />
            </intent-filter>
        </service>

        <service
            android:name=".tvservice.TvConnectionService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <service
            android:name=".torrent.TorrentServingService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!--
            El servicio de foreground de WorkManager viene de la librería SIN
            `foregroundServiceType`, y desde API 34 Android exige que el tipo que pide
            `setForeground()` esté declarado en el elemento <service>. Sin esto, la cola de
            descargas (LocalDownloadWorker, que pide DATA_SYNC) tira
            "foregroundServiceType 0x1 is not a subset of 0x0" y **mata la app**.

            OJO: no alcanza con el permiso FOREGROUND_SERVICE_DATA_SYNC, que ya estaba. Y
            tampoco alcanza con envolver `setForeground()` en un runCatching: la excepción no
            se lanza en la corrutina del worker sino después, en el hilo principal, dentro del
            propio SystemForegroundService. Verificado crasheando en un S24+ con Android 16.
        -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />

        <!--
            El banner va TAMBIÉN acá y no solo en <application>, que es lo que pide la doc de
            Android TV para la actividad de LEANBACK_LAUNCHER: sin esto, `aapt2 dump badging`
            mostraba `leanback-launchable-activity: ... banner=''`.

            OJO, que esto NO arregla el Fire TV Stick, y se probó: en el AFTKM (Fire OS /
            Android 11), con el banner ya resuelto en la actividad (`banner=0x7f0800f9` según
            `cmd package resolve-activity`) y tras un reinicio COMPLETO para descartar el caché
            del lanzador, la ficha del home siguió pintando el icono adaptativo con barras
            grises a los lados. El lanzador de Fire TV usa el ICONO para las apps sideloaded, no
            el banner. Por eso además existe mipmap-television/: ahí el icono que se le entrega
            en un TV ya es apaisado 16:9, que es la forma de la ficha. Ese camino quedó SIN
            verificar (hace falta otro reinicio para vaciar el caché del lanzador).
        -->
        <activity
            android:name=".MainActivity"
            android:banner="@drawable/ic_banner"
            android:configChanges="orientation|screenSize|keyboardHidden|smallestScreenSize|screenLayout"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.Arkiv.Starting">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.arkiv.player.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- El botón "Cancelar" de la notificación de descarga. `exported=false`: lo dispara solo
             nuestro propio PendingIntent, nadie de afuera. -->
        <receiver
            android:name=".data.local.AccionesDeDescargaReceiver"
            android:exported="false" />
    </application>
</manifest>
